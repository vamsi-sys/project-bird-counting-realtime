version: "3.9"
services:
  api:
    build: .
    container_name: bird_counting_api
    ports:
      - "8000:8000"
    volumes:
      - ./backend/uploads:/app/uploads
      - ./backend/outputs:/app/outputs
    restart: unless-stopped
